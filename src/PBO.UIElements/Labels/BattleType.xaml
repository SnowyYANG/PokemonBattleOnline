<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:c="clr-namespace:LightStudio.PokemonBattle.PBO.Converters"
                    xmlns:ui="clr-namespace:LightStudio.PokemonBattle.PBO.UIElements">
    <DataTemplate x:Key="BattleTypeLabel">
        <Border Background="{Binding Converter={x:Static c:BattleTypeBg.C}}" BorderBrush="{Binding Converter={x:Static c:BattleTypeBorder.C}}" BorderThickness="1" Height="17" MinWidth="35">
            <ContentControl Style="{x:Null}" Content="{Binding}" ContentTemplate="{x:Static ui:Controls.LocalizedText}" FontWeight="Normal" FontStyle="Normal" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding}" Value="Invalid">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>
